name: Deploy Backend

on:
  push:
    branches: [ main ]

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'



      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: gwon.my
          username: gwon
          port: 2222
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/gwon/gwon
            git fetch origin main
            git reset --hard origin/main

            # Mosquitto 설정 파일 자동 생성 (없을 경우에만)
            if [ ! -f ./mosquitto.conf ]; then
              echo "📄 mosquitto.conf 파일 생성 중..."
              echo "listener 1883" > mosquitto.conf
              echo "allow_anonymous true" >> mosquitto.conf
            fi

            cd backend
            chmod +x gradlew
            ./gradlew build -x test
            cd ..

            # Gradle 빌드 결과를 target 폴더로 복사 (Dockerfile 호환용)
            mkdir -p target
            cp backend/build/libs/*.jar target/

            # 기존 컨테이너 완전 정리 (포트 충돌 방지)
            docker stop $(docker ps -aq) 2>/dev/null || true
            docker rm $(docker ps -aq) 2>/dev/null || true

            # orphan 컨테이너도 정리
            docker compose down --remove-orphans || true

            # 컨테이너 및 네트워크 정리
            docker container prune -f
            docker network prune -f

            # 컨테이너 실행
            docker compose up --build -d
