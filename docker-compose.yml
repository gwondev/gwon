services:
  caddy:
    restart: always # ✅ 죽으면 다시 살리기
    image: caddy:alpine
    container_name : gwon-caddy
    ports:
      - "80:80"       # ✅ HTTP
      - "443:443"     # ✅ HTTPS
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - global_network

  backend:
    restart: always # ✅ 죽으면 다시 살리기
    container_name : gwon-backend
    build: ./backend
    environment:
      - DB_HOST=db
      - MQTT_BROKER=tcp://mosquitto:1883
    networks:
      - global_network


  frontend:
    restart: always # ✅ 죽으면 다시 살리기
    container_name : gwon-frontend
    build: ./frontend
    networks:
      - global_network

  mosquitto:
    restart: always # ✅ 죽으면 다시 살리기
    container_name : gwon-mosquitto
    image: eclipse-mosquitto:2
    ports:
      - "1883:1883"
      - "9001:9001"   # (옵션, WebSocket)
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    networks:
      - global_network



  db:
    restart: always # ✅ 죽으면 다시 살리기
    container_name : gwon-mysql
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: gwon
      MYSQL_USER: gwon
      MYSQL_DATABASE: gwon
      MYSQL_PASSWORD: gwon
    volumes:
      - db_data:/var/lib/mysql # ✅ 들여쓰기 맞게 수정
    networks:
      - global_network

volumes:
  caddy_data:
  caddy_config:
  db_data:


# 🌍 글로벌 네트워크 (다른 프로젝트와 공유)
networks:
  global_network:
    external: true
